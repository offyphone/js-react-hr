import React from "react";
import PropTypes from "prop-types";

const PostJob = props => {
  return (
    <div>
      <button>Post a new vacancy </button>
    </div>
  );
};

PostJob.propTypes = {};

export default PostJob;
